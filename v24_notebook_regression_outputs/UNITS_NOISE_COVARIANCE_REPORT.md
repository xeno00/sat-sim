# Units / Noise / Covariance Report

Status: `complete_static`

| source | term | count | classification | snippet |
| --- | --- | --- | --- | --- |
| V24.tex | sigma | 20 | unresolved_requires_stddev_vs_variance_check |   = \E{\RV{n}\RV{n}^\mathrm{T}} \in  \mathbb{R}^{N_{\mathrm{z}} \times N_{\mathrm{z}}}  \,. \end{IEEEeqnarray} Let $\V{\sigma}\in\mathbb{R}_{+}^{N_\mathrm{z}}$ denote the vector of range-domain standard deviations of the elements of $\RV{n}$; its entries may be selected according to the corresponding link conditions. Denote the $n$th meas |
| V24.tex | variance | 18 | unresolved_requires_stddev_vs_variance_check | exact DL transmission time (see Table \ref{tab:3gpp_signals} and Section \ref{subsec:signal_model}).} The measurement covariance is %with the measurement covariance defined as  \begin{IEEEeqnarray}{rCl} \label{eq:rician_cov_as_expectation} \M{R}_{\RV{z}} \triangleq \mathbb{C}\mathrm{ov}(\RV{z}) %\\ \nonumber %&=& %\E{(\RV{z}-\E{\RV{z}})(\ |
| V24.tex | R | 364 | unresolved | ks as IEEEtran.cls normally does \interdisplaylinepenalty=2500 \usepackage{tikz,pgfplots}   \pgfplotsset{compat=1.18} % Recommended by Overleaf  % WINS Lab added packages \usepackage[printonlyused,withpage]{acronym} \usepackage{cite} %Supports compressed, sorted lists of numerical citations \usepackage{color} \usepackage{comment} %Comment |
| V24.tex | SNR | 7 | safe_link_budget_context_if_units_checked | h \ac{LEO} satellites offer a promising alternative for reliable \ac{PNT} by providing diversity and increasing the \ac{SNR} over \ac{GNSS}.  However, the primary challenge in \ac{NTN}-based localization with \ac{LEO} satellites is that each satellite maintains a residual clock offset and drift without continuous constellation wide synchr |
| JCLS_Simulation.ipynb | 3e8 | 12 | safe_if_seconds_to_km_conversion_else_check_double_c |        self.clock_offset_seconds = float(clock_offset_seconds)         self.clock_offset_km = self.clock_offset_seconds*3e8/1000         self.sym_clock = sp.Symbol(f"delta_{self.node_id}", real=True)          self.tx_freq = float(f)    # in Hz         self.tx_bw = float(bw)     # in Hz         self.tx_power = float(p)   # in Watts         |
| JCLS_Simulation.ipynb | 1000 | 34 | safe_if_seconds_to_km_conversion_else_check_double_c |    self.clock_offset_seconds = float(clock_offset_seconds)         self.clock_offset_km = self.clock_offset_seconds*3e8/1000         self.sym_clock = sp.Symbol(f"delta_{self.node_id}", real=True)          self.tx_freq = float(f)    # in Hz         self.tx_bw = float(bw)     # in Hz         self.tx_power = float(p)   # in Watts         sel |
| JCLS_Simulation.ipynb | sigma | 32 | unresolved_requires_stddev_vs_variance_check | n = float(g)       # in abs, not dB ##### User Class class User(Node):     speed = 1 # meters per second     move_clock_sigma = 1e-8 # drift in seconds per second     def __init__(self, node_id, position, clock_offset_seconds):         """         User node which makes measurements.         Inherits from Node.         """         super(). |
| JCLS_Simulation.ipynb | variance | 102 | unresolved_requires_stddev_vs_variance_check | , -90 dBm          # Calculate SNR         snr = p_tx * g_tx * g_rx / (pathloss * p_n)          return snr      def get_variance_seconds(self):         snr = self.get_snr_abs()         if self.channel_type == "Rician":             return 1/(8*(np.pi*self.transmitter.tx_bw)**2*snr)         elif self.channel_type == "Rayleigh":              |
| JCLS_Simulation.ipynb | Sigma_z | 28 | unresolved | ate_error**2         P_true = np.diag(variances)         return P_true      def check_step_inputs(self, x, h_x, J_x, z, Sigma_z):         num_states = x.shape[0]         num_measurements = z.shape[0]         if J_x.shape != (num_measurements, num_states):             raise ValueError("Invalid Jacobian dimensions. expected", "(",num_measur |
| JCLS_Simulation.ipynb | R | 152 | unresolved |         self.clock_offset_km = self.clock_offset_seconds*3e8/1000      def connect(self, scenario):         """         Returns a list of all datalinks from this user to all other nodes in the scenario.         """         links = []         for other in scenario.nodes:             if other.node_id != self.node_id:                 link =  |
| JCLS_Simulation.ipynb | SNR | 15 | safe_link_budget_context_if_units_checked | smitter.gain         g_rx = self.receiver.gain         p_n = 1e-12  # Example noise power, -90 dBm          # Calculate SNR         snr = p_tx * g_tx * g_rx / (pathloss * p_n)          return snr      def get_variance_seconds(self):         snr = self.get_snr_abs()         if self.channel_type == "Rician":             return 1/(8*(np.pi*s |
| JCLS_Simulation.ipynb | bw | 31 | safe_link_budget_context_if_units_checked | #### Node Class class Node:     def __init__(self, node_id:int, position:np.array, clock_offset_seconds:float, f:float, bw:float, p:float, g:float):         """         Base class for a network node.         :param node_id: Unique identifier.         :param position: List or array representing the node's true position.         :param cloc |
| JCLS_Simulation.ipynb | clock_std_dev | 33 | unresolved_requires_stddev_vs_variance_check |         [1474.95950470806,	-5249.84507836376,	4455.15158752634]]      def __init__(self, num_users=3, num_satellites=3, clock_std_dev_seconds=1e-6, users=None, satellites=None):         """         The Scenario class stores global network information and manages the         dynamic estimation.         """         # Define the Scenario's n |
| jcls_sim\__init__.py | variance | 12 | unresolved_requires_stddev_vs_variance_check | onfig, tiny_v24_reproducibility_config, v24_crlb_mini_sweep_config from .bounds import (     average_clock_bound_from_covariance,     average_ue_peb_from_covariance,     clock_block_slice,     clock_parameter_indices,     clock_std_bounds_from_covariance,     covariance_from_fim,     manuscript_crlb_reportability_from_fim,     non_referen |
| jcls_sim\__init__.py | R | 4 | unresolved | """Core helpers for V24-consistent JCLS simulation code."""  from .constants import C_KM_PER_S, C_M_PER_S from .configs import V24ScenarioConfig, tiny_v24_reproducibility_config, v24_crlb_mini_sweep_config from .bounds import (     average_clock_bound_from_covariance,     average_ue_peb_from_covariance,     cl |
| jcls_sim\algorithm.py | 1000 | 2 | safe_if_seconds_to_km_conversion_else_check_double_c |    mean_satellite = np.mean(satellite_positions, axis=0)     norm = float(np.linalg.norm(mean_satellite))     if norm > 1000.0:         base = mean_satellite / norm * 6371.0         offsets = np.linspace(-0.15, 0.15, scenario.num_users)         return np.vstack([base + np.array([offset, -offset, 0.0]) for offset in offsets])     angles =  |
| jcls_sim\algorithm.py | sigma | 14 | unresolved_requires_stddev_vs_variance_check | on in range(1, max_iterations + 1):                 predictions = []                 jacobian_rows = []                 sigmas = []                 for row_index in row_indices:                     _receiver, transmitter = links[row_index]                     satellite_position = scenario.satellite_positions_km[transmitter - scenario.num_ |
| jcls_sim\algorithm.py | variance | 27 | unresolved_requires_stddev_vs_variance_check | e) class V24StateModel:     """V24 dynamic state model with explicit F, Q, and Pi."""      f_matrix: np.ndarray     q_covariance: np.ndarray     pi_matrix: np.ndarray     model_name: str = "theta_identity_state"      def validate(self, theta_dim: int) -> None:         """Validate model dimensions for a V24 theta dimension."""          sta |
| jcls_sim\algorithm.py | R | 17 | unresolved |     raise ValueError(f"pi_matrix must have shape ({theta_dim}, {state_dim}).")   @dataclass(frozen=True) class EstimatorResult:     """Estimator output and audit diagnostics."""      theta: np.ndarray     success: bool     diagnostics: dict[str, Any]     covariance: np.ndarray | None = None   def identity_theta_state_model(theta_dim: int, |
| jcls_sim\bounds.py | variance | 32 | unresolved_requires_stddev_vs_variance_check | """Bound extraction helpers for full-gauged V24 FIMs and covariances."""  from __future__ import annotations  import math  import numpy as np  from .gauge import expected_v24_parameter_dim   def _validate_counts(num_users: int, num_satellites: int) -> None:     """Validate posit |
| jcls_sim\bounds.py | R | 18 | unresolved |  at least 1, got {num_satellites}.")   def _validate_square_matrix(matrix: np.ndarray, name: str) -> np.ndarray:     """Return a validated square matrix."""      array = np.asarray(matrix, dtype=float)     if array.ndim != 2 or array.shape[0] != array.shape[1]:         raise ValueError(f"{name} must be a square matrix, got {array.shape}." |
| jcls_sim\configs.py | sigma | 4 | unresolved_requires_stddev_vs_variance_check |  dtype=float)         satellite_clocks = np.asarray(self.non_reference_satellite_clock_offsets_km, dtype=float)         sigmas = np.asarray(self.range_std_devs_km, dtype=float)          if ue_positions.shape != (self.num_users, 3):             raise ValueError(                 "ue_positions_km must have shape "                 f"({self.nu |
| jcls_sim\configs.py | R | 13 | unresolved |  raise ValueError(f"Invalid transmitter node id {transmitter_node_id}.")      def theta(self) -> np.ndarray:         """Return this scenario's V24 theta vector."""          self.validate()         theta = pack_v24_theta(             self.ue_positions_km,             self.ue_clock_offsets_km,             self.non_reference_satellite_clock_ |
| jcls_sim\constants.py | R | 2 | unresolved | """Physical constants used by the JCLS simulation package."""  C_M_PER_S = 299_792_458.0 C_KM_PER_S = 299_792.458  |
| jcls_sim\estimators.py | sigma | 9 | unresolved_requires_stddev_vs_variance_check | d_devs_km(range_std_devs_km: np.ndarray, expected_length: int) -> np.ndarray:     """Return range-domain precisions 1 / sigma**2."""      sigmas = np.asarray(range_std_devs_km, dtype=float)     if sigmas.ndim != 1:         raise ValueError(f"range_std_devs_km must be a 1-D array, got {sigmas.shape}.")     if sigmas.shape[0] != expected_le |
| jcls_sim\estimators.py | variance | 1 | unresolved_requires_stddev_vs_variance_check | td_devs_km: np.ndarray, ) -> tuple[np.ndarray, np.ndarray]:     """Return the information-form EKF posterior mean and covariance."""      x_pred_array = np.asarray(x_pred, dtype=float)     if x_pred_array.ndim != 1:         raise ValueError(f"x_pred must be a 1-D array, got {x_pred_array.shape}.")     state_dim = x_pred_array.shape[0]     |
| jcls_sim\estimators.py | R | 7 | unresolved | ns", ]   def _range_weights_from_std_devs_km(range_std_devs_km: np.ndarray, expected_length: int) -> np.ndarray:     """Return range-domain precisions 1 / sigma**2."""      sigmas = np.asarray(range_std_devs_km, dtype=float)     if sigmas.ndim != 1:         raise ValueError(f"range_std_devs_km must be a 1-D array, got {sigmas.shape}.")    |
| jcls_sim\figure_generation.py | sigma | 22 | unresolved_requires_stddev_vs_variance_check | _km from .metrics import clock_error_relative_to_reference, position_error_m from .noise import LinkBudgetConfig, range_sigmas_for_links from .parameters import pack_v24_theta, unpack_v24_theta  matplotlib.use("Agg") import matplotlib.pyplot as plt  # noqa: E402  SAT_SIM_ROOT = Path(__file__).resolve().parents[1] ARTIFACT_WARNING = "Diagn |
| jcls_sim\figure_generation.py | variance | 7 | unresolved_requires_stddev_vs_variance_check | alization,     deterministic_position_initialization_candidates,     dynamic_soft_information_refinement,     initial_covariance_from_linearization,     joint_lm_jcls, ) from .configs import V24ScenarioConfig, directed_sidelink_links, downlink_links from .constants import C_KM_PER_S from .fim import gaussian_fim_from_jacobian from .gauge  |
| jcls_sim\figure_generation.py | R | 86 | unresolved | m_jcls, ) from .configs import V24ScenarioConfig, directed_sidelink_links, downlink_links from .constants import C_KM_PER_S from .fim import gaussian_fim_from_jacobian from .gauge import expected_v24_parameter_dim, reference_satellite_node_id from .geometry import GroundReference, manuscript_candidate_geometry from .io import json_ready f |
| jcls_sim\figure_generation.py | clock_std_dev | 3 | unresolved_requires_stddev_vs_variance_check |        "base_seed",         "monte_carlo_trials",         "num_users_values",         "num_satellites_values",         "clock_std_devs_ns",         "refinement_epochs",     }     missing = sorted(required - set(config))     if missing:         raise ValueError(f"Figure config {path} is missing field(s): {missing}.")     validate_figure_id |
| jcls_sim\fim.py | sigma | 7 | unresolved_requires_stddev_vs_variance_check | py as np   def range_covariance_from_std_devs_km(range_std_devs_km: np.ndarray) -> np.ndarray:     """Return R_z = diag(sigma**2) from range-domain standard deviations."""      sigmas = np.asarray(range_std_devs_km, dtype=float)     if sigmas.ndim != 1:         raise ValueError(f"range_std_devs_km must be a 1-D array, got {sigmas.shape}." |
| jcls_sim\fim.py | variance | 6 | unresolved_requires_stddev_vs_variance_check | -information helpers for V24 JCLS simulations."""  from __future__ import annotations  import numpy as np   def range_covariance_from_std_devs_km(range_std_devs_km: np.ndarray) -> np.ndarray:     """Return R_z = diag(sigma**2) from range-domain standard deviations."""      sigmas = np.asarray(range_std_devs_km, dtype=float)     if sigmas. |
| jcls_sim\fim.py | R | 6 | unresolved | ations  import numpy as np   def range_covariance_from_std_devs_km(range_std_devs_km: np.ndarray) -> np.ndarray:     """Return R_z = diag(sigma**2) from range-domain standard deviations."""      sigmas = np.asarray(range_std_devs_km, dtype=float)     if sigmas.ndim != 1:         raise ValueError(f"range_std_devs_km must be a 1-D array, go |
| jcls_sim\gauge.py | R | 6 | unresolved | k_by_node is missing required node id(s): {missing}.")   def reference_satellite_node_id(num_users: int) -> int:     """Return the V24 reference satellite node id, Nu + 1."""      if num_users < 1:         raise ValueError(f"num_users must be at least 1, got {num_users}.")     return num_users + 1   def all_clock_node_ids(num_users: int,  |
| jcls_sim\geometry.py | 1000 | 7 | safe_if_seconds_to_km_conversion_else_check_double_c |       "longitude_deg": float(lon_deg),                 "altitude_m": float(alt_m),                 "ecef_km": (ecef_m / 1000.0).tolist(),             }         )     return np.asarray(positions_m, dtype=float) / 1000.0, metadata   def generate_starlink_like_visible_satellites(     *,     reference: GroundReference,     requested_satellite |
| jcls_sim\geometry.py | R | 9 | unresolved |  6_378_137.0 WGS84_F = 1.0 / 298.257_223_563 WGS84_E2 = WGS84_F * (2.0 - WGS84_F)   @dataclass(frozen=True) class GroundReference:     """Reference geodetic point for local scenario generation."""      latitude_deg: float     longitude_deg: float     altitude_m: float   @dataclass(frozen=True) class CandidateGeometry:     """Candidate UE/ |
| jcls_sim\io.py | R | 2 | unresolved | rt json from pathlib import Path from typing import Any  import numpy as np   def json_ready(value: Any) -> Any:     """Return a JSON-serializable representation of small diagnostic values."""      if isinstance(value, np.ndarray):         return value.tolist()     if isinstance(value, np.generic):         return value.item()     if isins |
| jcls_sim\jacobian.py | R | 3 | unresolved | _validate_satellite_positions(     satellite_positions_km: np.ndarray,     num_satellites: int, ) -> np.ndarray:     """Return validated known satellite positions."""      satellite_positions = np.asarray(satellite_positions_km, dtype=float)     expected_shape = (num_satellites, 3)     if satellite_positions.shape != expected_shape:       |
| jcls_sim\measurements.py | R | 6 | unresolved | """Range-domain TOA measurement helpers for V24 JCLS simulations."""  from __future__ import annotations  from collections.abc import Sequence  import numpy as np  from .gauge import reference_satellite_node_id   def _valid |
| jcls_sim\metrics.py | 1000 | 1 | safe_if_seconds_to_km_conversion_else_check_double_c | shape (3,) or (N, 3), got {true_positions.shape}.")     return np.linalg.norm(est_positions - true_positions, axis=1) * 1000.0  |
| jcls_sim\metrics.py | R | 6 | unresolved |  v24_clock_node_ids   def _selected_clock_ids(num_users: int, num_satellites: int, clock_ids: str) -> list[int]:     """Return node ids selected by a supported clock-error group."""      if clock_ids == "all_non_reference":         selected = v24_clock_node_ids(num_users, num_satellites)     elif clock_ids == "ue":         selected = list |
| jcls_sim\noise.py | 1000 | 6 | safe_if_seconds_to_km_conversion_else_check_double_c | st be positive.")     sigma_m = C_M_PER_S / math.sqrt(8.0 * (math.pi * beta_hz) ** 2 * snr_linear)     return sigma_m / 1000.0   def link_budget_for_range(     *,     range_km: float,     link_type: str,     config: LinkBudgetConfig, ) -> dict[str, float | str]:     """Return link-budget diagnostics and range-domain sigma for one link.""" |
| jcls_sim\noise.py | sigma | 52 | unresolved_requires_stddev_vs_variance_check | _loss_db     return received_dbm - noise_power_dbm(bandwidth_hz, noise_density_dbm_per_hz, noise_figure_db)   def range_sigma_km_from_snr(snr_linear: float, beta_hz: float) -> float:     """Return range-domain TOA standard deviation in km using manuscript-style beta/SNR formula."""      if snr_linear <= 0.0:         raise ValueError("snr_ |
| jcls_sim\noise.py | R | 12 | unresolved | import math from dataclasses import dataclass from typing import Any  import numpy as np  from .constants import C_KM_PER_S, C_M_PER_S   @dataclass(frozen=True) class LinkBudgetConfig:     """DL/SL link-budget assumptions for manuscript-candidate diagnostics."""      dl_frequency_hz: float = 2.2e9     dl_bandwidth_hz: float = 20.0e6     d |
| jcls_sim\noise.py | SNR | 2 | safe_link_budget_context_if_units_checked | ise_density_dbm_per_hz: float,     noise_figure_db: float,     implementation_loss_db: float, ) -> float:     """Return SNR in dB for a free-space link budget."""      received_dbm = transmit_power_dbm + transmit_gain_db + receive_gain_db - fspl_db(range_km, frequency_hz) - implementation_loss_db     return received_dbm - noise_power_dbm( |
| jcls_sim\parameters.py | R | 5 | unresolved | tellites must be at least 1, got {num_satellites}.")   def ue_position_param_names(num_users: int) -> list[str]:     """Return UE position parameter names in V24 order."""      if num_users < 1:         raise ValueError(f"num_users must be at least 1, got {num_users}.")     return [         f"{axis}_{user_id}"         for user_id in range |
